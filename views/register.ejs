<!DOCTYPE html>
<html lang="it">

<head>
  <meta charset="UTF-8" />
  <title>Registrazione - ComixCity</title>
  <link rel="stylesheet" href="/css/stile.css" />
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />
</head>

<body>
  <%- include('partials/navbar1') %>

    <section class="hero chi-siamo-hero">
      <div class="hero-content-chisiamo">
        <h1 data-aos="fade-up">Registrati</h1>
        <p data-aos="fade-up" data-aos-delay="300">
          Unisciti alla community di ComixCity!
        </p>
      </div>
    </section>

    <main class="register-section background-gradient">
      <div class="register-container" data-aos="fade-up">
        <!-- FEEDBACK: errori o conferme -->
        <% if (typeof errore !=="undefined" && errore) { %>
          <div class="errore" role="alert" aria-live="assertive">
            <%= errore %>
          </div>
          <% } %>
            <% if (typeof successo !=="undefined" && successo) { %>
              <div class="successo" role="status" aria-live="polite">
                <%= successo %>
              </div>
              <% } %>

                <form action="/register" method="POST" class="register-form">
                  <label for="username" data-aos="fade-right" data-aos-delay="100">Username</label>
                  <input type="text" id="username" name="username" required pattern="[a-zA-Z0-9_]{3,30}"
                    title="Solo lettere, numeri e underscore (_), 3-30 caratteri. No spazi, no caratteri speciali."
                    placeholder="Es: mario_rossi123" />
                  <small class="form-help">Solo lettere, numeri e _ ; da 3 a 30 caratteri.</small>

                  <label for="email" data-aos="fade-right" data-aos-delay="300">Email</label>
                  <input type="email" id="email" name="email" required pattern="[^@\s]+@[^@\s]+\.[^@\s]+"
                    title="Inserisci un'email valida" />

                  <label for="password" data-aos="fade-right" data-aos-delay="500">Password</label>
                  <input type="password" id="password" name="password" required
                    pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$"
                    title="Minimo 8 caratteri, almeno una maiuscola, una minuscola e un numero" />
                  <small class="form-help">Minimo 8 caratteri, almeno una maiuscola, una minuscola e un numero.</small>

                  <label for="ruolo" data-aos="fade-right" data-aos-delay="700">Registrati come:</label>
                  <select name="ruolo" id="ruolo" required data-aos="fade-left" data-aos-delay="800">
                    <option value="utente">Utente</option>
                    <option value="espositore">Espositore</option>
                  </select>

                  <button type="submit" class="btn" data-aos="zoom-in" data-aos-delay="900">
                    Registrati
                  </button>
                </form>

                <p class="login-link" data-aos="fade-up" data-aos-delay="1000">
                  Hai già un account? <a href="/login">Accedi qui</a>
                </p>
      </div>
    </main>

    <%- include('partials/footer') %>
      <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
      <script src="/js/script.js"></script>
</body>

</html>